{
  "identity": {
    "agentName": "openclaw_poster_maker",
    "displayName": "OpenClaw Poster Maker",
    "description": "Turns a short text prompt into a poster image (PNG) using the configured image provider.",
    "version": "1.0.0",
    "provider": {
      "organization": "openclaw-foundation"
    }
  },
  "capabilities": {
    "taskKinds": ["request"]
  },
  "io": {
    "inputs": [
      {
        "id": "request",
        "description": "Short text to illustrate as a poster (max 400 characters).",
        "contentType": "application/json",
        "required": true,
        "example": {
          "text": "a lighthouse at dawn"
        },
        "schema": {
          "type": "object",
          "required": ["text"],
          "properties": {
            "text": {
              "type": "string"
            }
          }
        }
      }
    ],
    "outputs": [
      {
        "id": "poster",
        "description": "Generated poster image.",
        "contentType": "image/png",
        "guaranteed": true
      }
    ]
  },
  "tags": [
    {
      "id": "text-to-image",
      "name": "Text to Image",
      "description": "Generate a PNG poster from a short text prompt."
    }
  ],
  "extensions": {
    "blocks": {
      "billingMode": "free",
      "pricePerTask": "0.000",
      "listing": "public"
    }
  },
  "runtime": {
    "handler": "./handler.ts",
    "handlerExport": "default",
    "concurrency": 1,
    "expectedInstances": 1,
    "maxRunningTimeSec": 90
  }
}
